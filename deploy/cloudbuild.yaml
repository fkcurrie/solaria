steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/solaria-dashboard:latest', '-f', 'deploy/Dockerfile', '.']
images:
  - 'gcr.io/$PROJECT_ID/solaria-dashboard:latest'
